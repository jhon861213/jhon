
let correo = prompt("ingrese correo");
const num1 = NaN;

    

if (correo != num1 && correo != (" ")) {
 
  
} else if (correo == endwith("cumbre.edu.bo")) {

    
} else {
    const randNum = Math.random();
    const num = Math.floor(Math.random()*11);
    const now = new Date();
          now.getDate(); 
    let mensaje ="correo no valido vuelva a intentarlo" 
    alert(mensaje.concat(`${Math.floor(Math.random()*11)} ${now.getDate()}`));

}

let pasword = prompt("ingrese contraseña");         

if (pasword > 8) {
    
} else if (pasword.startsWith($)) {
    const consulta = confirm("desea agregar un post");
} else {

    const randNum = Math.random();
    const num = Math.floor(Math.random()*11);
    const now = new Date();
          now.getDate(); 
    let mensaje ="correo no valido vuelva a intentarlo" 
    alert(mensaje.concat(`${Math.floor(Math.random()*11)} ${now.getDate()}`));
}
